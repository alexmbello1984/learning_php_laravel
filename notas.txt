CREANDO OBJETOS EN PHP

Vamos a tomar el miniframework y convertirlo en Programacion Orientado a Objetos.

miremos que del miniframework deberia ser un objeto

Mirando el archivo index.php

- primero esta la configuracion, de momento se queda asi.
- llamado al controlador

Analisis 

Cuando en el navegador se hace enter para ver home por ejemplo sucede lo siguiente.

Se genera una PETICION http a la pagina web

el helper controller lo que hace es manejar la peticion para luego llamar a un CONTROLADOR.

LA PETICION seria nuestro primer objeto

Creamos directorio library para almacenar las clases que sean reusables en toda la aplicacion.

Los controladores tambien se van a convertir en clases diferentes que sean Controladores.

Las vistas tambien van a ser parte de objetos, parte de la respuesta RESPONSE que le devolveremos al usuario.


El nombre de la clase debe ser el nombre del archivo

* Creamos la clase Request
por lo general las variables son protegidas o privadas
los metodos en su mayoria publicos 

De esta forma la clase tiene mas seguridad ya que podemos realizar algunas validacion que no se podrian hacer si el acceso fuera directo.

Los controladores deben tener accion
una accion por defecto y las que se requieran

